{
  "name": "alex-gimenez-romero-research",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:publications": "node scripts/sync-publications.mjs",
    "predev": "npm run sync:publications",
    "dev": "astro dev",
    "prebuild": "npm run sync:publications",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "@astrojs/sitemap": "^3.7.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
